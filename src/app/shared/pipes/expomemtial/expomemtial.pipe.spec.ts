import { ExpomemtialPipe } from './expomemtial.pipe';

describe('ExpomemtialPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpomemtialPipe();
    expect(pipe).toBeTruthy();
  });
});
